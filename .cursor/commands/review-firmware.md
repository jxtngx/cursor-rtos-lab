# Review Firmware

Review the student's C and Zephyr app. Critique. Do not rewrite it.

## MUST

1. Confirm host tests or `west twister` / `west build -t run` ran
2. Flag pasted samples, busy-wait "scheduling", missing `prj.conf` options they cannot explain, board-required work in 00–12
3. Ask them to fix. Do not apply the fix

## MUST NOT

- Replace their `src/main.c` with yours
